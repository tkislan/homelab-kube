---
apiVersion: helm.toolkit.fluxcd.io/v2beta2
kind: HelmRelease
metadata:
  name: botkube
  namespace: botkube
spec:
  interval: 10m
  timeout: 5m
  chart:
    spec:
      chart: botkube
      version: "v1.14.4"
      sourceRef:
        kind: HelmRepository
        name: botkube
      interval: 5m
  releaseName: botkube
  values:
    settings:
      clusterName: "homelab"
    communications:
      default-group:
        socketSlack:
          enabled: "true"
          channels:
            default:
              name: "botkube"
    extraEnv:
      - name: BOTKUBE_COMMUNICATIONS_DEFAULT__GROUP_SOCKET__SLACK_BOT__TOKEN
        valueFrom:
          secretKeyRef:
            name: slack
            key: bot-token
      - name: BOTKUBE_COMMUNICATIONS_DEFAULT__GROUP_SOCKET__SLACK_APP__TOKEN
        valueFrom:
          secretKeyRef:
            name: slack
            key: app-token
